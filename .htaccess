Options -Indexes
RewriteEngine On
RewriteBase /y/kham/

# Only serve actual static files directly (CSS, JS, images, fonts)
RewriteCond %{REQUEST_FILENAME} -f
RewriteRule ^ - [L]

# Route everything else (including directories) through the app
RewriteRule ^(.*)$ app/index.php?url=$1 [QSA,L]
